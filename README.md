# bundle (generated output — do not hand-edit)

This branch is a **generated release build** of Snip: the Bun backend, the
built Angular frontend, and the CLI, assembled into one deployable unit by
[`scripts/build-bundle.mjs`](https://github.com/Darkoyaki/aisdlc_day1_workshop_demo/blob/main/scripts/build-bundle.mjs)
on the `main` branch.

Never edit files here by hand — changes will be overwritten the next time the
build script runs. To change what ends up in this branch, edit `backend`,
`frontend`, or `cli` and re-run the build script from `main`.
